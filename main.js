const dotenv            = require('dotenv');
dotenv.config();

/*let processQs = require("./scripts/process.questions.js");
let postQs = require("./scripts/post.questions.js");

let fn = ["macedonian", "physics", "chemistry", "biology", "sociology", "philosophy", "history"]
let idn = [0, 3, 4, 5, 6, 7, 8];

for (let i = 0; i < 7; ++i) {
    setTimeout(() => {
        let FILE_NAME = fn[i];
        let ID = idn[i];
        postQs.post(FILE_NAME, ID);
    }, 200 * i);
}*/

// let count = require("./scripts/count.questions.js");

// processQs.process(FILE_NAME, ID);
// let test = require("./scripts/filter.questions.js");